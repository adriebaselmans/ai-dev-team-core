# Known Context

This file is a human-readable placeholder for durable context exports.

## Rules
- Treat this file as optional readable output, not the canonical write path.
- Keep the bare skeleton repository pristine.
- Generate or update this file only in a real project repo when a readable context export is needed.

## Current Baseline
- Structured memory may be stored under `framework/memory/records/` in a real project repo.
- Repository-specific knowledge may be stored under `framework/memory/repository-knowledge/`.
- Markdown memory files in the bare skeleton repo should remain placeholders until a real project uses this skeleton.
