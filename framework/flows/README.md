# Flow Files

This folder contains the current team state.

- `current-status.md`: current phase, owner, and next action.
- `../runtime/state.json`: machine-readable runtime state snapshot.
- `current-status.md` should be treated as a rendered view of runtime state, not a hand-edited source of truth.
