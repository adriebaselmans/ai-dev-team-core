# Runtime State

This directory is for mutable local orchestration state in bootstrapped projects.

- `state.json` is written by the CLI and ignored by git.
- Framework code and reusable contracts stay under `.ai-team/framework/`.
- Delete `state.json` when you want to clear the last local orchestration snapshot.
